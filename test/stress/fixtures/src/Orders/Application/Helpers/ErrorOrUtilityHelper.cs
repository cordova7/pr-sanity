// Rule: ErrorOr in a using namespace (not as a return type) — document detector behavior.
using ErrorOr.Utilities;

namespace Orders.Application.Helpers;

public class Helper
{
    public void Log() { }
}
